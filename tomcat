FROM ubuntu:16.04
RUN apt-get update
RUN apt-get install -y apache2
RUN echo "<html> Build this image using Docker file</html"> >/var/www/html/index.html
RUN apt-get clean
EXPOSE 80
CMD ["apache2ct1", "-D" , "FOREGROUND"]
